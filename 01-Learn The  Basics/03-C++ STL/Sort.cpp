#include<iostream>


void explainExtra(){

    // in c++ stl, the sorting thing can be done by using the sort fuction

    // sort(a, a+n);  [start, end)

     // In C++, the comp parameter in the sort function is a comparator function. It's used to define the criteria for sorting elements within a container.   

// Understanding the sort Function
// The sort function typically takes three arguments:

// Beginning iterator: Points to the first element to be sorted.   
// Ending iterator: Points to the element just after the last element to be sorted.   
// Comparator (optional): A function or functor that defines the comparison logic.
// You can provide a custom comp function to specify a different sorting order.

// The comp function should take two elements as arguments and return a boolean value:
// true if the first element should be placed before the second element.
// false otherwise.


}

using namespace std;
int main()
{
 return 0;
}